run = "cd frontend && npm install && npm run build && cd .. && pip install -r backend/requirements.txt && uvicorn backend.main:app --host 0.0.0.0 --port 8000"

[nix]
channel = "stable-24_05"

[env]
PYTHONPATH = "."

[[ports]]
localPort = 8000
externalPort = 80
